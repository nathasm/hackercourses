<li>
  <%= link_to story.title, story_link(story) %>
  <%= link_to 'Edit', edit_story_path(story), :class => 'btn' %>
  <%= link_to 'Remote', story_path(story), :method => :delete, :confirm => "Are you sure?", :class => 'btn' %>
</li>
